# Services

The wallpaper rotation uses a per-user systemd timer and service. The timer starts shortly after login and rotates approximately every 24 hours. The system remains fully usable offline because the starter wallpapers are embedded in the ISO.
