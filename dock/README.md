# Dock

`verby-dock` is a small Python/GTK3 bottom dock. It only shows launchers whose commands exist, reducing broken shortcuts and unnecessary processes. The default configuration is copied to each new user's `~/.config/verbyos/dock.json`.
