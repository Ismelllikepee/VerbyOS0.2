# Wallpapers

The build embeds original SVG wallpapers generated for VerbyOS. They are intentionally vector-based so the ISO does not need to fetch copyrighted artwork from random corners of the internet, a surprisingly effective way to avoid future licensing headaches.
